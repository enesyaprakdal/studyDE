module.exports = {
  /*   dbSelection: function (
    pDb,
    pQuery,
    pAnswer,
    pFunction_1,
    pList,
    pScore,
    pIndex,
    pFunction_2
  ) {
    pDb.find(pQuery).then(function (pAnswerData) {
      if (pAnswer === pAnswerData[0].ubersetzung) {
        pFunction_1;
        pScore += 5;
        pList.splice(pIndex, 1);
      } else {
        pFunction_2;
      }
      console.log(pAnswer);
      console.log(pAnswerData[0].ubersetzung);
    });
  }, */
};
